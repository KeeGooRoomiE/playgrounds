---
---
<div class="bg-default" aria-hidden="true"></div>

<style>
  /* Flat, matching the template's plain #f5f5f5 page — no decoration by default.
     Swap in a themed background from this folder on a per-playground basis instead. */
  .bg-default {
    position: fixed;
    inset: 0;
    z-index: -1;
    pointer-events: none;
    background: var(--bg);
  }
</style>
